<h1>List of photos</h1>
<form>
  <label for="browser_input">Image</label>
  <input id="browser_input" type="text" name="input_image" placeholder="Enter a url for the image...">
  <label for="browser_caption">Caption</label>
  <input id="browser_caption" type="text" name="input_caption" placeholder="Enter a caption for the photo...">
  <label for="browser_user_id">Image</label>
  <input id="browser_user_id" type="text" name="input_owner_id" placeholder="Enter an ID of a User">
  <button>Add photo</button>
</form>

<table border="1">
  <tr>
    <th>Image</th>
    <th>Caption</th>
    <th>Owner</th>
    <th>Posted</th>
    <th> </th>
  </tr>
  <% @list_of_photos.each do |a_photo|%>
    <tr>
      <td><img src="<%= a_photo.image %>"></td>
      <td><%= a_photo.caption %></td>
      <td><%= a_photo.poster.username %></td>
      <td><%= a_photo.created_at %></td>
      <td><a href="">Show details</a></td>
    </tr>
  <% end %>
</table>
